version: '3'
services:
  web:
    build: .
    ports:
     - "80:80"
  redis:
    image: "redis:alpine"
  neo4j:
    image: neo4j:3.0
    ports:
      - "7474:7474"
      - "7687:7687"
    volumes:
      - $HOME/neo4j/data:/data
      - $HOME/neo4j/logs:/logs
